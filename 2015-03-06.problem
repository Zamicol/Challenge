oneliner that takes two files, each with columnar data consisting of "key:value\n" where key can exist in both files (and multiple times in each file), sum the values of unique keys, spitting out a unique list of totals by key as well as a count of occurrences of a given key
